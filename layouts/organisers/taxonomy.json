[
    {{- range $index, $organiser := .Pages }}
    {{- if $index }}, {{ end }}
    {
        "title": "{{ .Title }}",
        {{- with .File }}
        "baseName": "{{ .ContentBaseName }}"
        {{- else }}
        "baseName": ""
        {{- end }}
    }
    {{- end }}
]
