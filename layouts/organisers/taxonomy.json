{
    "organisers": [
        {{- range $index, $organiser := .Pages }}
        {{- if $index }}, {{ end }}
        {
            "value": "{{ .File.ContentBaseName }}",
            "label": "{{ .Title }}",
            "fullName": "{{ .Params.fullName }}",
            "contact": "{{ .Params.contact }}"
        }
        {{- end }}
    ]
}
