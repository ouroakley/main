[
    {{- range $index, $venue := .Pages }}
    {{- if $index }}, {{ end }}
    {
        "title": "{{ .Title }}",
        "address": "{{ .Params.address }}",
        "slug": "{{ if .File }}{{ path.Base .File.Dir }}{{ else }}{{ .Title | urlize }}{{ end }}"
    }
    {{- end }}
]
